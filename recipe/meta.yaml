{% set name = "tinyrpc" %}
{% set version = "0.9.3" %}
{% set sha256 = "3915c7c5319059b07f0ca049c0ff6f151f547a6c763fb9b0054dc04cb6781cef" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  script:
    - python -m pip install --no-deps --ignore-installed .

requirements:
  build:
    - python
    - pip
    - six
  run:
    - python
    - six

test:
  imports:
    - tinyrpc
    - tinyrpc.dispatch
    - tinyrpc.protocols
    - tinyrpc.server
    - tinyrpc.transports

about:
  home: http://github.com/mbr/tinyrpc
  license: MIT
  summary: 'A small, modular, transport and protocol neutral RPC library that, among other things, supports JSON-RPC and zmq.'
  license_family: MIT
  # license_file: LICENSE # https://github.com/mbr/tinyrpc/pull/44
  summary: 'Simple, fast, extensible JSON encoder/decoder for Python'

extra:
  recipe-maintainers:
    - mariusvniekerk
